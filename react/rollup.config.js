const aa = require("./src/aa.js");

const build = {
  input: "./src/index.ts",
};

const development = {
  input: "",
};
console.log(aa);
console.log(process.cwd());

// const rollup = (a = true ? build : development);

// export default rollup;
